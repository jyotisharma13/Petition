
<p>All of these people supported our cause !</p>
<h3> Here is the signer list:</h3>
<ul class="signers-ul">
       {{#signers}}
       <li>
           {{#if url}}
           <a href="{{ url }}">{{ first }} {{last}}</a>
           {{else}}
           {{ first }} {{ last }} ,
           {{/if}}
           {{age}} years,
           {{#city}}
           <a href="signers/{{.}}">{{.}} </a>
           {{/city}}
       </li>
       {{/signers}}
   </ul>
<a class="go-back" href="/thanks">Go Back</a>
<a class="logout" href="/logout">Logout</a>
